# Lombok Configuration
# Allow Lombok to copy Spring's @Qualifier annotations to generated constructors
# This is needed when using @RequiredArgsConstructor with multiple beans of the same type
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
