# Java Formatting Rules
- Always wrap chained method calls (e.g., Fluent APIs like `GraphQlTester` or `WebTestClient`) so each method call is on a new line starting with a dot.
- Do not collapse these chains into a single line, even if they are within the 190-character limit.
